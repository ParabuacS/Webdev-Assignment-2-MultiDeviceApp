<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FreeWrite</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="icon" type="image/x-icon" href="/images/favicon.ico">
    <style>
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-white text-slate-900 min-h-screen flex flex-col">

    <nav class="sticky top-0 z-50 bg-white/80 backdrop-blur-md border-b border-slate-100">
        <div class="container mx-auto px-6 h-20 flex items-center justify-between">
            
            <div class="flex items-center gap-8">
                <a href="/" class="flex items-center gap-3 group">
                    <div class="bg-emerald-600 text-white w-10 h-10 flex items-center justify-center rounded-xl font-black text-xl shadow-lg shadow-emerald-200 group-hover:bg-emerald-700 transition-all">
                        FW
                    </div>
                    <span class="text-2xl font-black text-slate-900 tracking-tighter group-hover:text-emerald-700 transition-colors">
                        FREEWRITE
                    </span>
                </a>
                
                <div class="hidden md:flex gap-6 text-slate-600 font-semibold text-sm uppercase tracking-wider">
                    <a href="/" class="hover:text-emerald-600 transition-colors">Browse</a>
                    <a href="/write" class="hover:text-emerald-600 transition-colors">Write</a>
                </div>
            </div>

            <div class="flex items-center gap-6">
                <% if (currentUser) { %>
                    <div class="flex items-center gap-5">
                        <a href="/logout" class="text-sm font-bold text-slate-400 hover:text-rose-600 transition-colors">Logout</a>
                        
                        <a href="/dashboard" class="group flex items-center gap-3 bg-slate-50 pl-4 pr-1 py-1 rounded-full border border-slate-100 hover:border-emerald-200 transition-all">
                            <span class="text-sm font-bold text-slate-700 group-hover:text-emerald-700">Studio</span>
                            <div class="w-9 h-9 bg-white rounded-full border border-slate-200 overflow-hidden shadow-sm group-hover:ring-2 group-hover:ring-emerald-500/20 transition-all">
                                <img src="https://api.dicebear.com/7.x/avataaars/svg?seed=<%= currentUser %>" alt="avatar">
                            </div>
                        </a>
                    </div>
                <% } else { %>
                    <div class="flex items-center gap-4">
                        <a href="/login" class="text-sm font-bold text-slate-600 hover:text-emerald-600 transition-colors">Sign In</a>
                        <a href="/register" class="bg-emerald-600 text-white px-5 py-2.5 rounded-xl font-bold text-sm hover:bg-emerald-700 transition shadow-lg shadow-emerald-100">
                            Get Started
                        </a>
                    </div>
                <% } %>
            </div>

        </div>
    </nav>

    <div class="flex-grow">